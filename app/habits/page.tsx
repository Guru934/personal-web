"use client";
import { useEffect, useMemo, useState } from "react";
import { ArrowLeft, Check, Flame, Plus, Trash2 } from "lucide-react";
import "./habits.css";
type Habit={id:number;name:string;days:boolean[]};const week=["M","T","W","T","F","S","S"];const seed:Habit[]=[{id:1,name:"Study for 25 minutes",days:[true,true,false,true,true,false,false]},{id:2,name:"Code daily",days:[true,true,true,true,true,true,false]},{id:3,name:"Review flashcards",days:[true,false,true,true,false,false,false]}];
export default function HabitsPage(){const[habits,setHabits]=useState<Habit[]>(seed),[name,setName]=useState(""),[ready,setReady]=useState(false);useEffect(()=>{try{const x=localStorage.getItem("pos.habits");if(x)setHabits(JSON.parse(x))}finally{setReady(true)}},[]);useEffect(()=>{if(ready)localStorage.setItem("pos.habits",JSON.stringify(habits))},[habits,ready]);const add=()=>{if(name.trim()){setHabits(x=>[...x,{id:Date.now(),name:name.trim(),days:Array(7).fill(false)}]);setName("")}};const streak=(days:boolean[])=>{let n=0;for(let i=days.length-1;i>=0&&days[i];i--)n++;return n};const completion=useMemo(()=>habits.length?Math.round(habits.reduce((n,h)=>n+h.days.filter(Boolean).length,0)/(habits.length*7)*100):0,[habits]);return <main className="habits-page"><a href="/" className="back"><ArrowLeft size={16}/> Personal OS</a><p>LIFE</p><h1>Habits</h1><span>Build consistency with a small weekly check-in.</span><section className="habit-summary"><Flame size={18}/><b>{completion}%</b><small>weekly completion</small></section><section className="habit-list">{habits.map(habit=><article key={habit.id}><header><div><h2>{habit.name}</h2><small>{streak(habit.days)} day streak</small></div><button onClick={()=>setHabits(x=>x.filter(item=>item.id!==habit.id))}><Trash2 size={14}/></button></header><div className="days">{habit.days.map((checked,index)=><button key={index} className={checked?"checked":""} onClick={()=>setHabits(x=>x.map(item=>item.id===habit.id?{...item,days:item.days.map((day,i)=>i===index?!day:day)}:item))}>{checked&&<Check size={13}/>}<span>{week[index]}</span></button>)}</div></article>)}<div className="add"><input value={name} onChange={e=>setName(e.target.value)} onKeyDown={e=>e.key==="Enter"&&add()} placeholder="Add a habit..."/><button onClick={add}><Plus size={16}/> Add habit</button></div></section></main>}
