"use client";import {useRef,useState} from "react";import {ArrowLeft,Download,Upload} from "lucide-react";import "./backup.css";const keys=["pos.tasks","pos.topics","pos.notes","pos.resources","pos.sessions","pos.captures","pos.cards","pos.daily-review","pos.formulas","pos.media"];
export default function BackupPage(){const ref=useRef<HTMLInputElement>(null),[msg,setMsg]=useState("");const exp=()=>{const data=Object.fromEntries(keys.map(k=>[k,localStorage.getItem(k)]));const a=document.createElement("a");a.href=URL.createObjectURL(new Blob([JSON.stringify(data,null,2)],{type:"application/json"}));a.download=`personal-os-backup-${new Date().toISOString().slice(0,10)}.json`;a.click();setMsg("Backup downloaded")};const imp=(e:React.ChangeEvent<HTMLInputElement>)=>{const f=e.target.files?.[0];if(!f)return;const r=new FileReader();r.onload=()=>{try{const d=JSON.parse(String(r.result));keys.forEach(k=>{if(typeof d[k]==="string")localStorage.setItem(k,d[k])});setMsg("Backup restored—refresh the page")}catch{setMsg("Invalid backup file")}};r.readAsText(f)};return <main className="backup"><a href="/" className="back"><ArrowLeft size={16}/> Personal OS</a><p>SETTINGS</p><h1>Data backup</h1><span>Export your local workspace or restore it on another browser.</span><section><button onClick={exp}><Download size={16}/> Export backup</button><button onClick={()=>ref.current?.click()}><Upload size={16}/> Restore backup</button><input ref={ref} type="file" accept="application/json" onChange={imp}/>{msg&&<small>{msg}</small>}</section></main>}
