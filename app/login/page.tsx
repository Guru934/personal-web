"use client";
import { FormEvent, useState } from "react";
import { useRouter } from "next/navigation";
import { createSupabaseBrowserClient } from "../../lib/supabase-browser";
import "./login.css";
export default function LoginPage(){const router=useRouter();const[email,setEmail]=useState(""),[password,setPassword]=useState(""),[mode,setMode]=useState<"login"|"signup">("login"),[message,setMessage]=useState(""),[busy,setBusy]=useState(false);const submit=async(e:FormEvent)=>{e.preventDefault();setBusy(true);setMessage("");const client=createSupabaseBrowserClient();if(!client){setMessage("Supabase environment variables are not available in this deployment.");setBusy(false);return}const redirectUrl=`${window.location.origin}/auth/callback`;const result=mode==="login"?await client.auth.signInWithPassword({email,password}):await client.auth.signUp({email,password,options:{emailRedirectTo:redirectUrl}});if(result.error)setMessage(result.error.message);else if(mode==="login"){router.replace("/");router.refresh()}else setMessage("Account created. Check your email to confirm your account.");setBusy(false)};const reset=async()=>{if(!email.trim()){setMessage("Enter your email first.");return}const client=createSupabaseBrowserClient();if(!client)return;setBusy(true);const result=await client.auth.resetPasswordForEmail(email,{redirectTo:`${window.location.origin}/auth/update-password`});setMessage(result.error?.message??"Password reset email sent. Check your inbox.");setBusy(false)};return <main className="auth"><a href="/">← Personal OS</a><section><p>PERSONAL OS</p><h1>{mode==="login"?"Welcome back":"Create your account"}</h1><span>{mode==="login"?"Sign in to sync your workspace.":"Create an account for cloud persistence."}</span><form onSubmit={submit}><label>Email<input type="email" required value={email} onChange={e=>setEmail(e.target.value)}/></label><label>Password<input type="password" required minLength={6} value={password} onChange={e=>setPassword(e.target.value)}/></label><button disabled={busy}>{busy?"Working...":mode==="login"?"Sign in":"Sign up"}</button></form>{mode==="login"&&<button className="switch" onClick={reset}>Forgot password?</button>}{message&&<p className="message">{message}</p>}<button className="switch" onClick={()=>setMode(mode==="login"?"signup":"login")}>{mode==="login"?"Need an account? Sign up":"Already have an account? Sign in"}</button></section></main>}
