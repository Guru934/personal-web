"use client";
import { FormEvent, useState } from "react";
import { createSupabaseBrowserClient } from "../../lib/supabase-browser";
import "./login.css";
export default function LoginPage(){const[email,setEmail]=useState(""),[password,setPassword]=useState(""),[mode,setMode]=useState<"login"|"signup">("login"),[message,setMessage]=useState(""),[busy,setBusy]=useState(false);const submit=async(e:FormEvent)=>{e.preventDefault();setBusy(true);setMessage("");const client=createSupabaseBrowserClient();if(!client){setMessage("Supabase environment variables are not available in this deployment.");setBusy(false);return}const result=mode==="login"?await client.auth.signInWithPassword({email,password}):await client.auth.signUp({email,password});if(result.error)setMessage(result.error.message);else setMessage(mode==="login"?"Signed in. You can return to your workspace.":"Account created. Check your email if confirmation is enabled.");setBusy(false)};return <main className="auth"><a href="/">← Personal OS</a><section><p>PERSONAL OS</p><h1>{mode==="login"?"Welcome back":"Create your account"}</h1><span>{mode==="login"?"Sign in to sync your workspace.":"Create an account for cloud persistence."}</span><form onSubmit={submit}><label>Email<input type="email" required value={email} onChange={e=>setEmail(e.target.value)}/></label><label>Password<input type="password" required minLength={6} value={password} onChange={e=>setPassword(e.target.value)}/></label><button disabled={busy}>{busy?"Working...":mode==="login"?"Sign in":"Sign up"}</button></form>{message&&<p className="message">{message}</p>}<button className="switch" onClick={()=>setMode(mode==="login"?"signup":"login")}>{mode==="login"?"Need an account? Sign up":"Already have an account? Sign in"}</button></section></main>}
