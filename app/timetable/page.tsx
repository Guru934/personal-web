"use client";
import { useEffect, useState } from "react";
import { ArrowLeft, CalendarDays, Plus, Trash2 } from "lucide-react";
import "./timetable.css";
type Block={id:number;day:string;time:string;title:string;topic:string};const seed:Block[]=[{id:1,day:"Monday",time:"10:30",title:"JavaScript study",topic:"Functions"},{id:3,day:"Wednesday",time:"19:00",title:"Review & recall",topic:"Functions"}];const days=["Monday","Tuesday","Wednesday","Thursday","Friday","Saturday","Sunday"];
export default function TimetablePage(){const[blocks,setBlocks]=useState<Block[]>(seed),[form,setForm]=useState({day:"Monday",time:"09:00",title:"",topic:""}),[ready,setReady]=useState(false);useEffect(()=>{try{const x=localStorage.getItem("pos.timetable");if(x)setBlocks(JSON.parse(x))}finally{setReady(true)}},[]);useEffect(()=>{if(ready)localStorage.setItem("pos.timetable",JSON.stringify(blocks))},[blocks,ready]);const add=()=>{if(form.title.trim()){setBlocks(x=>[...x,{id:Date.now(),...form}]);setForm({...form,title:"",topic:""})}};return <main className="timetable-page"><a href="/" className="back"><ArrowLeft size={16}/> Personal OS</a><p>PLAN</p><h1>Timetable</h1><span>Plan study blocks and keep the next action visible.</span><section className="new-block"><select value={form.day} onChange={e=>setForm({...form,day:e.target.value})}>{days.map(day=><option key={day}>{day}</option>)}</select><input type="time" value={form.time} onChange={e=>setForm({...form,time:e.target.value})}/><input value={form.title} onChange={e=>setForm({...form,title:e.target.value})} placeholder="Block title"/><input value={form.topic} onChange={e=>setForm({...form,topic:e.target.value})} onKeyDown={e=>e.key==="Enter"&&add()} placeholder="Linked topic (optional)"/><button onClick={add}><Plus size={16}/> Add block</button></section><section className="week">{days.map(day=><div className="day" key={day}><h2><CalendarDays size={15}/>{day}</h2>{blocks.filter(block=>block.day===day).sort((a,b)=>a.time.localeCompare(b.time)).map(block=><article key={block.id}><time>{block.time}</time><span><b>{block.title}</b>{block.topic&&<small>{block.topic}</small>}</span><button onClick={()=>setBlocks(x=>x.filter(item=>item.id!==block.id))}><Trash2 size={14}/></button></article>)}{!blocks.some(block=>block.day===day)&&<em>Open time</em>}</div>)}</section></main>}
