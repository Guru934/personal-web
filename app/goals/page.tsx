"use client";
import { useEffect, useState } from "react";
import { ArrowLeft, Check, Plus, Trash2 } from "lucide-react";
import "./goals.css";
type Milestone={id:number;title:string;done:boolean};type Goal={id:number;title:string;description:string;milestones:Milestone[]};
const seed:Goal[]=[{id:1,title:"Become competent in Web Development",description:"Build a reliable foundation through deliberate practice.",milestones:[{id:11,title:"Complete JavaScript fundamentals",done:false},{id:12,title:"Build three portfolio projects",done:false}]}];
export default function GoalsPage(){const[goals,setGoals]=useState<Goal[]>(seed),[title,setTitle]=useState(""),[description,setDescription]=useState(""),[milestones,setMilestones]=useState<Record<number,string>>({}),[ready,setReady]=useState(false);useEffect(()=>{try{const x=localStorage.getItem("pos.goals");if(x)setGoals(JSON.parse(x))}finally{setReady(true)}},[]);useEffect(()=>{if(ready)localStorage.setItem("pos.goals",JSON.stringify(goals))},[goals,ready]);const addGoal=()=>{if(title.trim()){setGoals(x=>[...x,{id:Date.now(),title:title.trim(),description,milestones:[]}]);setTitle("");setDescription("")}};const addMilestone=(id:number)=>{if(milestones[id]?.trim()){setGoals(x=>x.map(g=>g.id===id?{...g,milestones:[...g.milestones,{id:Date.now(),title:milestones[id].trim(),done:false}]}:g));setMilestones(x=>({...x,[id]:""}))}};return <main className="goals-page"><a href="/" className="back"><ArrowLeft size={16}/> Personal OS</a><p>PLAN</p><h1>Goals</h1><span>Turn long-term outcomes into milestones you can actually finish.</span><section className="new-goal"><input value={title} onChange={e=>setTitle(e.target.value)} placeholder="Goal title"/><input value={description} onChange={e=>setDescription(e.target.value)} placeholder="Why does this matter?" onKeyDown={e=>e.key==="Enter"&&addGoal()}/><button onClick={addGoal}><Plus size={16}/> Add goal</button></section><div className="goal-list">{goals.map(goal=>{const completed=goal.milestones.filter(x=>x.done).length;const progress=goal.milestones.length?Math.round(completed/goal.milestones.length*100):0;return <article className="goal-card" key={goal.id}><header><div><h2>{goal.title}</h2><p>{goal.description}</p></div><button onClick={()=>setGoals(x=>x.filter(g=>g.id!==goal.id))}><Trash2 size={15}/> Delete</button></header><div className="progress"><i style={{width:`${progress}%`}}/></div><small>{progress}% complete · {completed}/{goal.milestones.length} milestones</small>{goal.milestones.map(item=><div className="milestone" key={item.id}><button className={item.done?"done":""} onClick={()=>setGoals(x=>x.map(g=>g.id===goal.id?{...g,milestones:g.milestones.map(m=>m.id===item.id?{...m,done:!m.done}:m)}:g))}>{item.done&&<Check size={13}/>}</button><span className={item.done?"strike":""}>{item.title}</span><button onClick={()=>setGoals(x=>x.map(g=>g.id===goal.id?{...g,milestones:g.milestones.filter(m=>m.id!==item.id)}:g))}><Trash2 size={13}/></button></div>)}<div className="milestone-add"><input value={milestones[goal.id]||""} onChange={e=>setMilestones(x=>({...x,[goal.id]:e.target.value}))} onKeyDown={e=>e.key==="Enter"&&addMilestone(goal.id)} placeholder="Add milestone..."/><button onClick={()=>addMilestone(goal.id)}><Plus size={14}/></button></div></article>})}</div></main>}
