"use client";
import { useState } from "react";
import { ArrowLeft, Database, RotateCcw, ShieldCheck } from "lucide-react";
import "./settings.css";
const keys=["pos.tasks","pos.topics","pos.notes","pos.resources","pos.sessions","pos.captures","pos.cards","pos.daily-review","pos.formulas","pos.media","pos.goals","pos.timetable","pos.habits","pos.exam"];
export default function SettingsPage(){const[message,setMessage]=useState("");const clear=()=>{if(window.confirm("Clear all Personal OS data from this browser? Export a backup first if you may need it.")){keys.forEach(key=>localStorage.removeItem(key));setMessage("Workspace reset. Reload any open pages.")}};return <main className="settings"><a href="/" className="back"><ArrowLeft size={16}/> Personal OS</a><p>SETTINGS</p><h1>Workspace settings</h1><span>Control the local-first data used by your Personal OS.</span><div className="settings-grid"><section><article><Database size={18}/><div><h2>Local-first storage</h2><p>Your tasks, notes, study sessions, and planning data are saved in this browser only.</p></div></article><article><ShieldCheck size={18}/><div><h2>Private by default</h2><p>No account or server sync is active yet. Your data is not sent anywhere by this app.</p></div></article></section><section className="danger"><h2>Reset workspace</h2><p>Delete all local Personal OS data from this browser. This cannot be undone unless you have exported a backup.</p><a href="/backup">Open backup first</a><button onClick={clear}><RotateCcw size={15}/> Reset local data</button>{message&&<small>{message}</small>}</section></div></main>}
